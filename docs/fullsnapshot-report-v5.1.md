# v5.1 corpus 全量快照报告

日期:2026-09-06 深夜 | 工具:`scripts/corpus_fullscan.py` | 依据:issue #1(spec)、ADR-0004 增补

## 执行账目

| 项 | 值 |
|---|---|
| 范围 | 星空旗舰版(93,=AI 星空)+ 苍穹(87);global=false + productIds 过滤(global=true 时过滤被上游忽略,实测) |
| 词表 | 163 词(宽词+域词+症状词,`data/fullscan-terms.json`) |
| 上游请求 | **3,148**(红线豁免上限 7,500 的 42%),1 请求/秒,耗时 90 分钟 |
| 产出 | **81,765 次 stub 写入 → 去重后 65,523 篇唯一文档** |
| corpus 现状 | **65,557 篇 / 97MB**(knowledge 12,010 / answer 37,076 / article 16,471 / usage 0) |

## 验收(对照 issue #1)

1. **首次检索全面性**:rg 冒烟(零上游)——「信用额度」688 篇命中、「分子分母」255 篇命中;冷门域从"词表首轮 1,209 篇"进入"全量 6.5 万篇"量级;
2. **金标覆盖率**:usage 层 8.7% → **21.7%**(5/23);reference 层 16.9%(不变——其金标多为星瀚 MC/课程/发版说明,本就在 93+87 范围之外,由交接文档 18 的 school 通道与 issue #2 的帮助中心通道补齐,预期内);
3. **在线管线透明性**:发现层只写 corpus,不触碰检索管线(与 discovery-report-v5.md 结论一致);
4. **红线**:3,148 ≤ 7,500(豁免额度),1 请求/秒全程;**豁免已用毕**,下次全量刷新需重新拍板;
5. **幂等/断点**:verify 假服务检查通过(首轮+3/二轮 0);进度文件 `data/fullscan-progress.json` 支持续跑。

## 边界(诚实声明)

- "全量"=**163 词匹配集的并集**,上游按相关度+时间序返回;词表外措辞的文档可能仍有遗漏。验证方式(需重新拍板请求额度):间隔数日重跑 fullscan,新增量≈0 即证明覆盖饱和;
- stub 只有标题+摘要;正文仍按"rg 命中 → kd read 写穿"按需增长(不变量:上游被请求,语料才更新);
- usage 沉淀数为 0——corpus 深度引擎(每次解题会话收尾写 usage 文件)尚未启动,这是下一次真实使用时的动作项。

## 后续

- 交接文档 18:school 子系统(schoolapi/search 匿名可用)→ 课程+发版说明接入;
- issue #2:帮助中心 DokuWiki 批量通道(官方文档补充);
- 时间网格 discovery_sweep.py 保留,转日常增量(≤200 请求/轮)。
