# 去服务化 — 文档与代码残留引用参考清单

> 用途:执行「文档同步」与「删旧代码」工单时的穷尽式索引,避免漏改导致文档指向不存在的能力。
> 来源:2026-09-17 只读探查(全仓 grep + 实测环境)。**此文件是内部清点底稿,不对外承诺能力。**

## 一、环境实测事实(优先级最高,与文档表述冲突时以本节为准)

| 对象 | 实测 | 文档说法 |
|---|---|---|
| `~/.lingeebuild/releasenotes/` | **目录不存在**(`ls` exit=2,find 零命中) | ADR-0011 称「为空(0 篇)」——不精确 |
| `~/.lingeebuild/` | **存在**,但仅含 `landing/`(37 文件) | — |
| `~/.kingdee-kit` | **不存在** | ADR-0011 称「为空」——不精确 |
| `~/.agents/skills` | **不存在** | 同上 |
| 服务 `127.0.0.1:4097` | **当时在跑**(v6.2) | — |

即:ADR-0011 与交接文档里的「均为空」表述,实际是「不存在」。凡引用该说法处应校正为准确表述。

## 二、`KSEARCH_URL` 的特殊性

16 处引用,**没有任何一处声明它已废除**。ADR-0008:24 反而写「`kd` 只剩一个环境变量 `KSEARCH_URL`」——该表述在去服务后已错。这是清单里唯一的「隐性失效项」。

## 三、分能力引用点(去重后:文档 + 代码,不含历史日志)

| 能力 | 处数 | 必须改的重点位置 |
|---|---|---|
| `kd manifest` 字符串 | 17 | `README.md` 13/24/43/72/73/97/111、`SKILL.md:29`、`install.sh:86`、`install.ps1:125`、`cli/kd.py:148/238` |
| `/manifest` 端点 | 15 | 同上重叠 + `tests/verify_ksearch.py` 59/66/178/262 |
| 落地缓存 / landing | 32(另有 58 处历史日志可留) | `README.md:39`、`SKILL.md` **第 8 行(front-matter,最易漏)**、16/155/159/161/166/171/215/245/251/270/273、`.gitignore` |
| corpus / `KSEARCH_CORPUS` | 约 25 | `.gitignore:45-46`、`SKILL.md:185`、`data/fullscan-*.json` |
| `ksearch.db` / `KSEARCH_DB` | 约 15 | `.gitignore:43`、`README.md:40` |
| `semantic_rerank` / 语义重排 | 约 60 | `install.sh:27/64-69`、`install.ps1:33/86/90`、`docs/tickets/21-*.md` |
| `KSEARCH_URL` | 16 | `README.md:117`、`install.sh:62`、`install.ps1:115`、`docs/adr/0008:24` |
| 十个 HTTP 端点 | 约 120 | service 内约 60、tests 内约 40、文档约 20 |
| `start-service.*` | 8 | **`cli/kd.py:52` 与 `:241`(用户可见的修复指引,最易漏)** |
| `run_eval` / `data/eval` | 约 20 | `README.md:42`、`CONTEXT.md:96`(词条**未标废除**) |
| `docs/API.md` | 5 处引用 | 文件本体 85 行,存在,待整体删除 |

## 四、SKILL.md 第 2 步整节边界

文件 `skills/kingdee-knowledge/skills/kingdee-knowledge/SKILL.md`,共 274 行。

- **待删整节 = 第 149–167 行(含)**;下一节起点 = **第 169 行**
- 149 节标题 / 151–156 引言+两行目录表 / 158–162 bash 块(三条 rg) / 164–167 四条要点 / 168 空行分隔
- 删除时保留一个空行再接 169
- **节外易漏的关联点**:第 8 行(front-matter `description`)、第 16–17 行、215、245、251、270、273

**两处口径需同步改**(ADR-0011:65-66 只提「整节作废」,未提标题):

- 第 22 行 `## kd 的定位(三级回退)` → 三级变两级
- 第 34 行 `## ⭐ 检索流程(四步,不要临场发挥)` → 四步变三步

## 五、`releasenotes` 「承诺了不存在的能力」的位置

| 位置 | 状态 |
|---|---|
| `CONTEXT.md:51-52` | 已诚实标注待办(措辞「为空库」需校正为「不存在」) |
| `README.md:41` | **零标注**,写成可用特性 → 必须改 |
| `SKILL.md:156/160/252` | **零标注**,且 160 教用户对不存在的目录跑 `rg` → 必须改 |

## 六、处置判据

- **必须改**:面向用户/agent、会导致其按不存在的能力行动(命令、路径、环境变量、流程口径)
- **可保留**:ADR / specs / research / tickets 里的历史决策叙述——**历史 ADR 不改写**,新决策由 ADR-0011 承载
- **同名不同物,勿误删**:`scripts/releasenotes_ingest.py` 的 `manifest` 是摄取断点文件,与 `kd manifest` 无关;`docs/eval-*.md` 里的 `rerank=false` 是**信号重排**,与**语义重排**(`semantic_rerank`)是两回事
