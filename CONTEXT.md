# kingdee-knowledge-kit

金蝶官方知识的本地匿名检索套件。第一用户是 agent(个人客户端里的 AI),人类几乎不直接使用;`kd` CLI 是唯一入口,检索内核是可 import 的库。v6 主轴:**彻底在线**——模仿人类在论坛解决问题,用时在线搜、搜到即读即查,预囤只保留发版说明(ADR-0005)。

## Language

**kd**:
本套件的 CLI 二进制名。AI-first:stdout 只出 JSON 数据、进度走 stderr、错误是带 hint 的 JSON、永不交互。
_Avoid_: kingdee-cli、kdk

**检索内核**:
封装金蝶云社区匿名接口的**可 import 库**(`kd.core`),`kd` CLI 与其同进程。唯一事实源——改能力或适配上游变更都只改它。
~~检索服务(kingdee-ksearch-service)、常驻 HTTP 服务~~ **已废除(2026-09-16,ADR-0011)**:HTTP 层与十个端点删除,不留兼容层。服务化唯一的技术理由(避免重载昂贵资源)在本项目从未激活——重排模型默认关且从未下载,而 CLI 冷启动实测 0.09–0.15 秒。
_Avoid_: L1、后端、server(泛称)、kingdee-ksearch-service(已废除的形态)

**三种实体**:
检索结果的三个类型 `knowledge`(官方文档)/ `answer`(社区问答帖)/ `article`(社区文章),`type` 字段贯穿 search 与 read,一一对应。
_Avoid_: 三级链路、知识类型

**两级检索路由**:
v6 的问题解决路径:answer+knowledge **并行**多路搜索(`kd ask` 一次带回);本地精查层随去服务化取消,原第三跳由**站外搜索**承担(ADR-0011)。展示排序 answer 优先(症状近似度最高),knowledge 补根因解释。
_Avoid_: 串行三层、answer 万能论(本案例证明正确答案可以是 knowledge)

**排序模式(sortsType)**:
上游检索的排序开关。`0/1` = 相关性排序,`2/3` = 时间倒序(新→旧)。相关性排序下老文档可居首,时间倒序对「老文档被编辑」与「长尾低流量文档」是结构性盲区——原句路固定用 1,采集脚本曾误用 2(ADR-0009)。
_Avoid_: 排序权重、排序规则(泛称,指代不明)

**资料包**:
`kd ask` 的返回物:多路并行检索命中 + 按相关度深读的 topK 全文,合成回答所需的全部素材;ask 是唯一常规入口,search/read 是手动细粒度命令。
_Avoid_: RAG 结果、上下文

**多路关键词**:
ask 内置的检索策略:症状词路 + 字段/实体名词路 + 产品词路 + **原句路**(≤7 路 RRF 融合)。存在意义是跨越**词汇鸿沟**(症状描述与文档命名的词法错位,如「分母变平方」↔「生产单位数量」)。
原句路与片段路信息独立、同权重参与融合(实测原句排第 1 而片段融合排 23,ADR-0009),**不是兜底路**。
_Avoid_: 同义词扩展(泛称)、检索词改写(旧 SKILL 术语,已升级为内置行为)

**落地缓存(landing)**:
~~查询路径深读写穿落盘的本地文档目录,查过的毫秒级复用,支持 rg 字段名级精查。~~
**已废除(2026-09-16,ADR-0011)**:本地落盘缓存整体取消(含 `corpus/`、`~/.lingeebuild/landing`、
`data/ksearch.db`)。理由:重复问同一件事的概率低,维护成本高于收益。**连带后果**——原 v6「三级回退」
的中间层(`rg` 精查)随之作废,退化为两级。
_Avoid_: 语料库、索引语料(见下条,两者红线圈不同)

**索引语料(Index Corpus)**:
~~为本地检索专门采集的语料池:无偏、可重复全量重建,给本地检索一个不受使用史污染的分母。~~
**已废除(2026-09-16,ADR-0011)**:本地落盘体系整体取消,本词条及其与落地缓存的配对区分
(更新机制/偏置属性/红线圈)一并失去依据。
_Avoid_: 语料(泛称,三义混用是历史事故)、corpus(v5 旧义:预爬语料,已废除)、训练集

**发版说明库**:
~~唯一预囤语料:`~/.lingeebuild/releasenotes/`,93 旗舰版近 1 年,一版本一 md + front-matter,正文保留官方「模块-问题-修复」结构。~~
**已取消(2026-09-16,ADR-0011)**:该目录**从未建立**——`~/.lingeebuild/` 下只有遗留的 `landing/`,
`releasenotes/` 子目录整个不存在(不是「空库」)。本地预囤体系整体取消后,「官方已修复」类问题
改由 `kd ask` 召回 + 站外搜索官方发版说明承担,**引用时须以官方原文为准,不得凭印象断言修复版本**。
_Avoid_: 更新日志(泛称)、changelog

**回答规范(ANSWER-SPEC)**:
合成回答必须遵循的格式单一事实源:原因分析→分步方案→操作边界、表格、[n] 编号引用、资料未覆盖时诚实声明;根因解释优先引 knowledge/发版说明,answer 引用用于症状对齐。
_Avoid_: 输出模板、prompt(泛称)

**匿名链路**:
全链路零账号、零 cookie、零官方点数。频率档位(v6):交互会话短突发 2-3 请求/秒+随机抖动,后台/摄取任务 1 请求/秒;激进高频已评估并否决(封禁 = 免费链路死亡)。
_Avoid_: 免登录(含免费注册义)、爬虫、高频无抖动

**降级(fallback)**:
~~kd ai 的模型通道不可用时,自动改为返回资料包并带 `fallback:true`。~~
**已废除(2026-09-16,ADR-0008)**:`kd ai` 命令删除,套件不再持有模型通道,整条链路零模型依赖。资料包本就是唯一产物,不存在"降级"这一态。
_Avoid_: 失败、报错(原义即:降级不是错误)

**合成权**:
回答由**调用方 agent**合成,不由本套件合成。主路 = 用户 → 当前对话的 agent → `kd ask` 拿资料包 → 子代理按规范合成 → 返回「答案 + 结构化引用」(ADR-0008)。子代理**只合成不做检索**。合成规范的**单一事实源是 `docs/ANSWER-SPEC.md`**(见 README「规范在哪」),本词条只声明规范位置,不复制规范内容;把规范交付给子代理的责任由 `SKILL.md` 提示词模板承担。
_Avoid_: kd ai(已删除的命令)、回答器(套件不是)

**召回置信度**:
合成方对本次召回是否足以回答的**三档判定**:`answerable`(有段落直接说明机制/字段/步骤)/ `partial`(有相关材料但未覆盖所问)/ `uncovered`(内容不匹配或未命中)。须把客观召回信号(`fusedScore`/命中路数/`budget_exhausted`)一并喂入判定方。**禁止用 0-1 分数**——LLM 无校准、量纲冲突、且分数诱导过度自信(ADR-0010)。
_Avoid_: 置信分数、score、确定性(指代不明)

**模型通道**:
~~kd ai 合成回答用的 OpenAI 兼容端点(KAI_BASE/KAI_MODEL)。~~
**已废除(2026-09-16,ADR-0008)**:`KAI_BASE`/`KAI_MODEL` 环境变量移除。
**后续补正(2026-09-17,ADR-0011)**:`KSEARCH_URL` 亦已废除——内核进程内直连,无 HTTP/无端口;
`KSEARCH_INDEX` 同样失效(sqlite 上游缓存随去服务化删除)。`kd` 现无任何必需环境变量。
_Avoid_: LLM 服务、AI 服务(泛称)

**chunk 切片**:
把文档全文按标题感知(【问题描述】/【概述】/【操作步骤】等结构)切成带序号与标题的段落。`/ask` 每源附 top3 相关 chunk,供合成方定位命中段落、不引与结论无关的全文。
**现状注记(2026-09-16)**:chunk 携带的 `seq` 是**本地切片序号**,不是官方 chunkId——`[n](chunk#m)` 的 m 无法经 `kd read <chunkId> --chunk` 还原。该引用粒度依赖官方 chunkId 映射(下条),当前暂缓。
_Avoid_: 分段(泛称)、embedding(切片不向量化)

**评测集**:
~~`data/eval/evalset.json`,金标 = usage 层(真实使用产生的问题,gold=解题过程读全文核实的文档)。~~
**已废除(2026-09-16,ADR-0011)**:`data/eval/` 全目录与 `scripts/run_eval.py` 随去服务化删除,
28 例双层金标不复存在。**验收改以两条手工硬指标为准**:**词汇鸿沟存活率**(症状词↔字段名错位用例必过)、
**根因可解释率**(回答须引到根因段落,只引 answer 不过);回归由 `tests/kd_regression.py` 承担
(回归≠评测:前者防退化,后者量质量)。
_Avoid_: 测试集(verify 是回归,评测是质量)、官方对齐

**词汇鸿沟**:
用户症状语言与官方文档命名之间的词法错位。v6 的头号敌人:路由靠多路关键词硬编码、评测靠词汇鸿沟存活率盯防,不再依赖 agent 临场改写。
_Avoid_: 语义鸿沟(同义,但统一用本词)

**chunk 溯源**:
引用的目标形态:`标题 + entityId + chunkId`,匿名经 `GET /aisapi/document-chunks/{chunkId}` 实时解析 chunk 全文(2026-09-06 匿名 body 级实测 200)。**永不发网页 URL**——vip.kingdee.com 网页路由(question/article/knowledge)匿名一律 302 登录墙,是引用 404 的根因。
**现状与边界(2026-09-16)**:
- 官方**无「按文档列出全部 chunk」端点**(`?documentId=` 实测 404),`document-chunks/{id}` 只做**反查**;
- chunkId 的唯一来源是**登录态** SSE 终止帧的 `searchSources[].id`(其中 `entityId` 才是文档 ID)与官方分享对话正文;
- chunkId 形态为自增短整型(如 2659901),**同文档内连续、跨文档不单调映射到 entityId**,故不可枚举、不可推导;
- 结论:**该能力未落地,当前暂缓**。`kd read --chunk` 消费端已就绪,缺的是 `entityId → chunkId` 映射的产出端。
_Avoid_: 引用链接(旧义:网页 URL)、link(泛称)

**查询内语义重排**:
~~每次 ask 对拉回的候选做本地轻量语义打分(bge-small-zh ONNX,~100MB),只作用于 topK 深读选择。~~
**已废除(2026-09-16,ADR-0011)**:`semantic_rerank` 模块删除。它要解决的问题(关键词搜不到但语义相关)
**已由 ADR-0009 的原句路解决**——后者零依赖、已上线、有实测背书(原句排第 1 vs 片段融合第 23)。
而该模块默认关、模型从未下载、需额外两个依赖。**将来若重新启用语义重排,冷启动重载模型会反向要求服务化**,
须重新评估 ADR-0011。
_Avoid_: 向量库(已降级待墙)、embedding 索引(无落盘)、语义搜索(夸大:不改变召回集合,只改深读顺序)

**教师模式**:
用用户本人登录态低频采样官方 AI(/aisapi/ai-search SSE),把全流(管线步骤/思考流/引用清单)落盘为评测金标与检索轨迹;官方 AI 的定位由此固化:**教师 + 金标,永不做在线引擎**。工具链已侦察实锤(见 docs/research/2026-09-06-official-ai-frontend-recon.md)。风险已被用户知悉并拍板暂不设限;执行时仍守人类频率与自有账号边界。
_Avoid_: 官方 API 接入(暗示依赖)、白嫖官方引擎(定位错误)

---

_历史词条(v5,已废除):corpus 语料目录、缓存语料库、发现层、fullscan/sweep——见 ADR-0004/0005 的取舍记录,勿再使用。_
