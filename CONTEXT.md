# kingdee-knowledge-kit

金蝶官方知识的本地匿名检索套件。第一用户是 agent(个人客户端里的 AI),人类几乎不直接使用;kd CLI 是唯一入口,检索服务是唯一事实源。v6 主轴:**彻底在线**——模仿人类在论坛解决问题,用时在线搜、搜到即读即查,预囤只保留发版说明(ADR-0005)。

## Language

**kd**:
本套件的 CLI 二进制名。AI-first:stdout 只出 JSON 数据、进度走 stderr、错误是带 hint 的 JSON、永不交互。
_Avoid_: kingdee-cli、kdk

**检索服务(kingdee-ksearch-service)**:
唯一内核/唯一事实源,本地封装金蝶云社区匿名接口;改能力或适配上游变更都只改它。
_Avoid_: L1、后端、server(泛称)

**三种实体**:
检索结果的三个类型 `knowledge`(官方文档)/ `answer`(社区问答帖)/ `article`(社区文章),`type` 字段贯穿 search 与 read,一一对应。
_Avoid_: 三级链路、知识类型

**三层检索路由**:
v6 的问题解决路径:answer+knowledge **并行**多路搜索;发版说明库第三跳兜底(前两层空手或无法解释根因时才触发)。展示排序 answer 优先(症状近似度最高),knowledge 补根因解释。
_Avoid_: 串行三层、answer 万能论(本案例证明正确答案可以是 knowledge)

**资料包**:
`kd ask` 的返回物:多路并行检索命中 + 按相关度深读的 topK 全文(已写穿落地缓存),合成回答所需的全部素材;ask 是唯一常规入口,search/read 是手动细粒度命令。
_Avoid_: RAG 结果、上下文

**多路关键词**:
ask 内置的检索策略:症状词路 + 字段/实体名词路 + 产品词路(≤6 路 RRF 融合)。存在意义是跨越**词汇鸿沟**(症状描述与文档命名的词法错位,如「分母变平方」↔「生产单位数量」)。
_Avoid_: 同义词扩展(泛称)、检索词改写(旧 SKILL 术语,已升级为内置行为)

**落地缓存(landing)**:
查询路径深读写穿落盘的本地文档目录(一文档一 md + front-matter)。**查询历史的沉淀**,不是覆盖性语料;rg 对它做字段名/报错原文的全文精查。查过的毫秒级复用,没查过的不存在。
偏置属性:**被问过**——只含查询路径上真实命中的文档,分布由使用史塑造,不可当作全量快照做覆盖率统计。
_Avoid_: corpus(旧义:预爬语料,已废除)、语料库、索引语料(见下条,两者红线圈不同)

**索引语料(Index Corpus)**:
为本地检索**专门采集**的语料池:无偏、可重复全量重建、含明确采集口径与词表来源。存在的理由是给本地检索一个不受使用史污染的分母(覆盖率统计、召回评测、BM25/向量基线的共同底料)。
与落地缓存的边界按**三处**区分:更新机制(重建 vs 随用随长)、偏置属性(无偏 vs 被问过)、红线圈(采集体量触及上游抓取条款 vs 单账号低频影子使用)。
重建与否由测得的上游实际覆盖数 **M** 决定,M 未知时 `本地篇数/M` 不可计算。
_Avoid_: 语料(泛称,三义混用是历史事故)、corpus(v5 旧义:预爬语料,已废除)、训练集

**发版说明库**:
唯一预囤语料:`~/.lingeebuild/releasenotes/`,93 旗舰版近 1 年(先试),一版本一 md + front-matter(产品/版本/日期),正文保留官方「模块-问题-修复」结构。**「官方已修复」类问题的终审依据**,也是前两层解释不了根因时的第三跳。
_Avoid_: 更新日志(泛称)、changelog

**回答规范(ANSWER-SPEC)**:
合成回答必须遵循的格式单一事实源:原因分析→分步方案→操作边界、表格、[n] 编号引用、资料未覆盖时诚实声明;根因解释优先引 knowledge/发版说明,answer 引用用于症状对齐。
_Avoid_: 输出模板、prompt(泛称)

**匿名链路**:
全链路零账号、零 cookie、零官方点数。频率档位(v6):交互会话短突发 2-3 请求/秒+随机抖动,后台/摄取任务 1 请求/秒;激进高频已评估并否决(封禁 = 免费链路死亡)。
_Avoid_: 免登录(含免费注册义)、爬虫、高频无抖动

**降级(fallback)**:
kd ai 的模型通道不可用时,自动改为返回资料包并带 `fallback:true`;任何情况下都有可用数据。
_Avoid_: 失败、报错(降级不是错误)

**模型通道**:
kd ai 合成回答用的 OpenAI 兼容端点(KAI_BASE/KAI_MODEL),接用户自己的渠道;与服务无关、可缺席。
_Avoid_: LLM 服务、AI 服务(泛称)

**chunk 切片**:
把文档全文按标题感知(【问题描述】/【概述】/【操作步骤】等结构)切成带序号与标题的段落。`/ask` 每源附 top3 相关 chunk,合成引用可到 `[n](chunk#m)`,对齐官方 AI 的 chunk 级引用粒度。
_Avoid_: 分段(泛称)、embedding(切片不向量化)

**评测集**:
`data/eval/evalset.json`,金标 = usage 层(真实使用产生的问题,gold=解题过程读全文核实的文档)。v6 起从零重建(旧金标随语料删除,首条种子见 ADR-0005「评测」节),两条硬指标:**词汇鸿沟存活率**(症状词↔字段名错位用例必过)、**根因可解释率**(回答须引到根因段落,只引 answer 不过)。彻底在线后评测消耗上游:用例池化+抽样跑;**先标金、后测管线**(防自证循环)。
_Avoid_: 测试集(verify 是回归,评测是质量)、官方对齐

**词汇鸿沟**:
用户症状语言与官方文档命名之间的词法错位。v6 的头号敌人:路由靠多路关键词硬编码、评测靠词汇鸿沟存活率盯防,不再依赖 agent 临场改写。
_Avoid_: 语义鸿沟(同义,但统一用本词)

**chunk 溯源**:
引用的标准形态:`标题 + entityId + chunkId`,匿名经 `GET /aisapi/document-chunks/{chunkId}` 实时解析 chunk 全文(2026-09-06 匿名 body 级实测 200)。**永不发网页 URL**——vip.kingdee.com 网页路由(question/article/knowledge)匿名一律 302 登录墙,是引用 404 的根因。落地缓存 front-matter 记 entityId/chunk 映射;`kd read --chunk <id>` 为手动细粒度入口。
_Avoid_: 引用链接(旧义:网页 URL)、link(泛称)

**查询内语义重排**:
向量召回在 v6 的唯一形态:每次 ask 对拉回的候选做**本地轻量语义打分**(bge-small-zh ONNX,~100MB,零上游零 API 成本),**只作用于 topK 深读选择**(决定哪些文档被深读——ask 链路的咽喉);展示排序仍守三层路由的 answer 优先规则。无预囤、无向量落盘、离线缓存不向量化。
_Avoid_: 向量库(已降级待墙)、embedding 索引(无落盘)、语义搜索(夸大:不改变召回集合,只改深读顺序)

**教师模式**:
用用户本人登录态低频采样官方 AI(/aisapi/ai-search SSE),把全流(管线步骤/思考流/引用清单)落盘为评测金标与检索轨迹;官方 AI 的定位由此固化:**教师 + 金标,永不做在线引擎**。工具链已侦察实锤(见 docs/research/2026-09-06-official-ai-frontend-recon.md)。风险已被用户知悉并拍板暂不设限;执行时仍守人类频率与自有账号边界。
_Avoid_: 官方 API 接入(暗示依赖)、白嫖官方引擎(定位错误)

---

_历史词条(v5,已废除):corpus 语料目录、缓存语料库、发现层、fullscan/sweep——见 ADR-0004/0005 的取舍记录,勿再使用。_
